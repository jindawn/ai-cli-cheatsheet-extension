# AI CLI 速查表：完整安装与故障排查

本文档说明浏览器扩展的安装、使用和源码维护流程。

当前扩展包含 18 个工具分类、1500 余条数据，准确数量以 `node tools/validate-data.js` 的输出为准。

## 1. 安装浏览器扩展

### 放到固定目录

Chrome Web Store 版面向普通用户，会由浏览器自动更新，并提供本地查询、收藏、偏好和个性化推荐。这些基础功能不需要本机桥接，也不会调用模型。

源码版以“加载已解压的扩展程序”方式运行，目录移动或被清理后会失效。不要放在 Downloads、桌面临时目录或云盘临时同步目录。

可以直接克隆仓库：

macOS / Linux：

```bash
git clone https://github.com/jindawn/ai-cli-cheatsheet-extension.git ~/workspace/ai-cli-cheatsheet-extension
```

Windows：

```powershell
git clone https://github.com/jindawn/ai-cli-cheatsheet-extension.git C:\workspace\ai-cli-cheatsheet-extension
```

也可以下载 ZIP，解压到固定目录。

### 加载到 Chrome / Edge

1. Chrome 打开 `chrome://extensions/`，Edge 打开 `edge://extensions/`。
2. 开启右上角“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择包含 `manifest.json` 的项目根目录。
5. 在浏览器扩展菜单中固定“AI CLI 速查表”。

基础查询到这里即可使用，不需要安装额外程序，也不会请求网络。

## 2. 基础使用

- 输入命令、中文用途或英文说明进行跨工具搜索。
- 使用工具标签和“快捷键 / 命令 / 参数”标签筛选。
- 点击结果行复制首选平台对应的命令；其他平台专属内容始终可见，首次跨平台复制时会确认目标环境。
- 点击“用法”展开高频示例、操作场景和注意事项。
- 使用“最近”和“收藏”快速访问常用条目。
- 在“管理”页设置首选命令平台、跨平台启用或隐藏工具，并查看数据来源；平台选择不会隐藏整个工具分类。

快捷键：

| 快捷键 | 功能 |
|--------|------|
| `/` 或 `Cmd/Ctrl+K` | 聚焦搜索框 |
| `Esc` | 清空搜索或退出搜索框 |
| `↑` / `↓` | 在结果之间移动 |
| `Enter` | 复制当前结果 |

## 3. 可选：查询并新增与检查更新

只有主动使用“数据维护与高级操作”时才需要本机桥接：

1. 在管理页展开“数据维护与高级操作”，点击“检测”，再从桥接返回的动态 AI 环境列表中选择执行环境。
2. 若未安装或版本过旧，界面只会提供对应系统的固定版本 `1.7.6` 图形安装包；不需要复制或执行安装命令。安装包自带运行时，不要求另装 Python。
3. 安装完成后返回扩展点击“重新检测”；正常情况下不需要重启浏览器。
4. 若所选 CLI 未登录，请按该工具自身的正常方式完成登录后重新检测。
5. 点击“查询并新增”或某个工具的“检查官方更新”，核对预计使用的环境与用量提示后确认。
6. 查看官方清单、来源、场景审校和风险差异；确认后才应用，或随时取消/放弃。

扩展不会自动换用另一个模型。桥接未安装、权限被拒绝或被卸载后，基础查询、收藏和本地推荐仍可正常使用。

## 4. 维护内置数据（贡献者）

普通用户不需要安装任何本地程序。仓库贡献者如需修改或新增数据，必须先阅读 [`OFFICIAL_DATA_POLICY.md`](OFFICIAL_DATA_POLICY.md)，并同步维护数据文件、官方清单、来源登记和场景审校快照。

基本流程：

1. 按照 [数据 Schema](data/SCHEMA.md) 修改或创建 `data/<tool-id>.js`。
2. 同步更新 `shared/official-inventories/`、`shared/scenario-reviews/`、`shared/source-registry.json` 和相关适配器。
3. 运行仓库规定的 JavaScript 与 Python 校验。
4. 重新加载已解压的扩展进行人工检查。

源码版可运行 `native-host/install.sh` 或 `native-host/install.ps1` 注册开发桥接；脚本读取当前扩展 ID。正式商店用户应使用 GitHub Release 中已签名、公证且绑定商店 ID 的安装包。

若源码版已安装 v1.7.5 的桥接，更新仓库后无需重装：它会继续读取只含 Claude、Codex、Gemini 和 OpenCode 的兼容基础清单。Qwen 的一键安装与其他 v5 能力仅由新版桥接按需提供。

已验证的源码发布包和商店版会在 Qwen Code 卡片上提供“升级检测组件”按钮。点击后只需在系统图形安装界面确认一次，再回到扩展点“重新检测”；扩展会继续 Qwen 的安装准备，不需要终端命令或浏览器重启。直接加载的源码工作区在签名安装包尚未发布前不会显示失效下载链接。

## 5. 常见问题

### 如何更新扩展本身

- Chrome Web Store 版由浏览器自动更新。
- Git 克隆的源码版运行 `git pull`，再到扩展管理页点击“重新加载”。
- ZIP 源码版下载新包覆盖原目录，再重新加载。

### 从源码版切换到商店版

两者通常使用不同的扩展 ID，因此本地收藏、最近使用和偏好不会自动迁移。确认商店版可用后，可手动卸载旧的已解压版本。

### macOS 在 Downloads 目录中出现权限错误

将项目移动到 `~/workspace/` 或 `~/Documents/` 等固定目录，再在浏览器中重新加载。

## 6. 隐私说明

基础查询不包含网络请求。搜索词、收藏、最近 20 条复制记录、首选命令平台、跨平台复制确认和启用工具偏好保存在浏览器本地存储中。

只有用户确认维护任务后，工具名称、现有数据、确定性官方清单、场景审校和生成要求才会发送给其明确选择的 AI 环境。常见环境会在“添加 AI 环境…”中列出；已验证适配器可直接检测，其他本机工具只需输入名称并通过一次风险确认后尝试通用调用。项目不会通过额外服务器中转请求；账号凭据由各 CLI 或桥接本地配置持有，不写入扩展存储。

用户确认的通用调用只保存名称和 PATH 中的可执行文件名到本机桥接目录，不同步也不会上传；它以无额外参数、标准输入传递任务，并要求标准输出返回结构化 JSON。它不接受路径、任意 Shell 命令、管道、重定向或模型返回内容执行。通用模式并不保证 CLI 只读；不兼容、未登录或非 JSON 输出都会停止任务且不写入数据。桥接不会在冷启动时检查更新；签名目录或本地配置变化后必须重新检测并确认环境，旧任务摘要不能继续执行。

返回 [README](README.md)。
