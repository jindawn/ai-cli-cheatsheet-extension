# AI CLI 速查表隐私政策

生效日期：2026 年 7 月 28 日

AI CLI 速查表的基础查询、收藏和个性化推荐仅在用户的浏览器中运行，不向项目维护者、项目服务器或模型服务传输数据。可选的“查询并新增”和“检查官方更新”只有在用户安装本机桥接、明确选择 AI 环境并再次确认任务后才会调用模型。

## 本地处理的数据

扩展会在浏览器本地保存以下内容，用于提供速查和偏好设置：

- 搜索词和所选筛选条件。
- 收藏的条目标识。
- 最近复制的条目和命令，最多 20 条。
- 首选命令平台、已启用工具、跨平台复制确认和引导状态等偏好。
- 已忽略的推荐。
- 用户选择的 AI 执行环境、桥接安装向导进度和已确认应用的动态数据修订。

这些数据保存在 `chrome.storage.local` 中，不跨设备同步。扩展不读取浏览历史、网页内容、Cookie、账号信息或身份信息。本地推荐只使用已启用工具、收藏和最近使用记录排序。

## 权限与联网

Chrome Web Store 包安装时只申请 `storage` 权限。只有用户点击桥接检测时才申请可选的 `nativeMessaging` 和 `alarms`；只有应用完整动态修订时才申请 `unlimitedStorage`。拒绝这些权限不会影响基础查询、收藏或本地推荐。

扩展本身不执行模型返回的代码。模型输出必须经过普通 JSON 解析、分块长度与 SHA-256 校验、官方清单闭合、来源登记、场景审校和两阶段存储回读后才可成为本地覆盖。

## 数据共享与用途

项目不收集、出售、出租、分享或用于广告、信用评估及其他与速查功能无关的用途。

当用户确认维护任务时，本机桥接会把工具名称、当前数据集、已核验的官方清单与资料、场景审校和生成要求发送给用户明确选择的 AI 环境。该环境可以是内置或签名适配器支持的本机 CLI、用户明确确认的本地自定义适配器，也可以是用户在桥接侧配置的 OpenAI/Anthropic 兼容 API。数据直接流向用户自己配置的模型服务，不经过项目服务器。实际处理受对应服务的隐私条款约束。

桥接只记录所用 Provider、CLI 版本和模型标识，不把账号、Token 或凭据返回给扩展。凭据仍由各 CLI 管理；兼容 API 的端点、模型、Token 和桥接运行日志只保存在本机桥接目录。扩展仅保存不含凭据的 Provider ID。

## 保留与删除

本地数据会保留到用户在扩展中清空对应记录，或卸载扩展并删除其浏览器存储数据。

## 本机桥接说明

商店版与源码版使用同一维护协议。商店安装包的 Native Messaging `allowed_origins` 只包含固定商店扩展 ID；源码版安装脚本读取实际扩展 ID。扩展无法静默安装或启动未注册的本机程序。

Provider 协议使用目录摘要绑定用户选择。扩展不会接受任意可执行命令；常见工具可通过内置或签名适配器接入，清单外工具只需输入名称，由桥接在 PATH 中检测；找不到时才补充实际命令名。

用户完成一次风险确认后，桥接会先做一次有界的试调用，在其**自带的固定调用方式**中找出该工具能响应的一种：每种方式最多 60 秒、标准输入始终关闭，全部失败则不保存任何配置。扩展只能提交一个调用方式的标识，永远不能提交参数本身；保存下来的参数数组直接取自桥接内置常量，任务提示只替换其中一个独立的数组元素。该配置只包含名称、PATH 文件名和调用方式标识，不同步、不上传，也不含 Token；它不保证 CLI 只读，任务前仍会再次确认模型用量。通用调用不接受路径、Shell 字符串、管道、重定向、命令替换或模型返回内容执行；非 JSON 输出、超时、未登录或失败时不会写入数据，也不会切换到其他模型。签名目录仅在用户主动检测或维护时刷新。

## 联系方式

如对本政策有疑问，请通过 [GitHub Issues](https://github.com/jindawn/ai-cli-cheatsheet-extension/issues) 联系项目维护者。
